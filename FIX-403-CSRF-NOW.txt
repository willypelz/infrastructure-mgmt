╔══════════════════════════════════════════════════════════════════╗
║          JENKINS 403 CSRF ERROR - IMMEDIATE FIX                  ║
╚══════════════════════════════════════════════════════════════════╝

You're getting: "HTTP 403 No valid crumb was included in the request"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚨 IMMEDIATE FIX (Do this NOW):

1. CLEAR YOUR BROWSER CACHE & COOKIES
   ─────────────────────────────────────
   This is THE MOST IMPORTANT step!

   Chrome/Firefox/Edge:
   → Press: Ctrl+Shift+Delete (or Cmd+Shift+Delete on Mac)
   → Select: "Cookies and other site data"
   → Time range: "All time"
   → Click: "Clear data"

   OR use Private/Incognito window:
   → Ctrl+Shift+N (Chrome)
   → Ctrl+Shift+P (Firefox)

2. RESTART JENKINS
   ─────────────────────────────────────
   Run this command on your server:

   docker restart jenkins

   Wait 30 seconds

3. ACCESS JENKINS IN INCOGNITO/PRIVATE WINDOW
   ─────────────────────────────────────
   https://jenkins.gmcloudworks.org

   Login and try creating a new item

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 WHY THIS HAPPENS:

Your browser cached old CSRF tokens before the fix was applied.
Even though Jenkins is now configured correctly, your browser is
sending old/invalid tokens, causing 403 errors.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ WHAT'S ALREADY FIXED (from running fix-jenkins.sh):

✓ Jenkins upgraded to Java 21
✓ CSRF protection enabled in jenkins-casc.yaml:
  security:
    crumbIssuer:
      standard:
        excludeClientIPFromCrumb: true
✓ Jenkins URL configured
✓ Reverse proxy settings updated

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 IF CLEARING CACHE DOESN'T WORK:

Option 1: Reload Jenkins Configuration
───────────────────────────────────────
docker exec jenkins curl -X POST http://localhost:8080/reload
docker restart jenkins

Option 2: Manual UI Fix
───────────────────────────────────────
1. Access Jenkins in incognito window
2. Go to: Manage Jenkins → Security → Configure Global Security
3. Under "CSRF Protection":
   ✓ Check "Prevent Cross Site Request Forgery exploits"
   ✓ Select "Default Crumb Issuer"
   ✓ Check "Enable proxy compatibility"
4. Click "Save"
5. Restart Jenkins: docker restart jenkins

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 QUICK TEST:

After clearing cache and restarting Jenkins:

1. Open incognito/private window
2. Go to: https://jenkins.gmcloudworks.org
3. Login
4. Click "New Item"
5. Try to enter a name and click pipeline

SUCCESS = No 403 error! ✅
STILL FAILS = Try Option 1 or 2 above

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

TL;DR - DO THIS NOW:

1. Clear browser cache (Ctrl+Shift+Delete, clear ALL)
2. docker restart jenkins
3. Wait 30 seconds
4. Access Jenkins in incognito window
5. Try creating new item - should work! ✅

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
